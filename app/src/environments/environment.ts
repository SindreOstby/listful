// Production config. The publishable key is safe to ship to the browser; never put the secret key here.
export const environment = {
  supabaseUrl: 'https://zuavucwsgcwbtbxnofir.supabase.co',
  supabasePublishableKey: 'REPLACE_WITH_PROJECT_PUBLISHABLE_KEY',
};
